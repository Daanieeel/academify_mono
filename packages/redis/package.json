{
  "name": "@repo/redis",
  "private": true,
  "type": "module",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "dev": "echo \"Redis package has no dev server\"",
    "build": "tsc -b",
    "test": "echo \"No tests yet for redis\"",
    "lint": "bunx oxlint -c ../config/oxlint/base.json --node-plugin --import-plugin --promise-plugin --deny-warnings .",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "ioredis": "^5.10.1"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^6.0.3"
  },
  "peerDependencies": {
    "typescript": "^6"
  }
}
