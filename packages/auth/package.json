{
  "name": "@repo/auth",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "dev": "echo \"Auth package has no dev server\"",
    "build": "tsc -b",
    "test": "echo \"No tests yet for auth\"",
    "lint": "bunx oxlint -c ../config/oxlint/base.json --import-plugin --promise-plugin --deny-warnings .",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@better-auth/expo": "^1.6.20",
    "@repo/database": "workspace:*",
    "better-auth": "^1.6.20"
  },
  "devDependencies": {
    "typescript": "catalog:"
  }
}
