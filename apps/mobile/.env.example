# Gateway URL the app talks to. `localhost` works for iOS Simulator; for a
# physical device or Android emulator, use your machine's LAN IP instead
# (e.g. http://192.168.1.23:3001) — the device can't resolve "localhost" as
# your dev machine.
EXPO_PUBLIC_API_URL=http://localhost:3001
